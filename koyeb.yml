name: telegram-uploader-bot
regions:
  - fra
services:
  - name: uploader
    type: web                   # 👈 tells Koyeb it’s NOT a web app
    ports: []                      # 👈 no open ports = no health check
    env:
      - key: BOT_TOKEN
        value: "123456:ABCDEF-your-bot-token"
    dockerfile: ./Dockerfile
    mounts: []
